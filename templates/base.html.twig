<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <header>
        <nav class="navbar navbar-light bg-light">
            <a class="navbar-brand" href="/">Home</a>
        </nav>
    </header>
    <body>
    {% if app.user %}
        Bonjour {{ app.user.email }} !
        <a href="{{ path('app_logout') }}" style="background-color: beige"> Se déconnecter</a>
    {% else %}
        <a href="{{ path('app_login') }}" style="background-color: beige"> Se connecter</a>
    {% endif %}
        {% block body %}{% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>
